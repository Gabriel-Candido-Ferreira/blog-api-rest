# === Configurações do MongoDB ===
# URI de conexão com o MongoDB.
# Use "mongo" se estiver usando Docker (nome do serviço no docker-compose).
# Use "localhost" se estiver rodando localmente sem Docker.
MONGO_URI=mongodb://mongo:27017/authdb

# === Configurações do JWT ===
# Chave secreta usada para assinar tokens JWT.
JWT_SECRET=your_super_secret_jwt_key_1234567890abcdef

# Tempo de expiração do token JWT (ex: 1h, 30m, 7d).
JWT_EXPIRES=1h

# === Configurações da Aplicação ===
# Porta em que a aplicação irá rodar.
PORT=3000

# === Configurações do PostgreSQL ===
# Usuário do banco de dados PostgreSQL.
DB_USER=seu_usuario_postgres

# Senha do banco de dados PostgreSQL.
DB_PASSWORD=sua_senha_segura

# Nome do banco de dados PostgreSQL.
DB_NAME=nome_do_banco_postgres

# Host do banco de dados PostgreSQL.
# Use "postgres" se estiver usando Docker (nome do serviço no docker-compose).
# Use "localhost" se estiver rodando localmente sem Docker.
DB_HOST=postgres

# === Configurações do PGAdmin ===
# E-mail padrão para login no PGAdmin.
PGADMIN_DEFAULT_EMAIL=admin@admin.com

# Senha padrão para login no PGAdmin.
PGADMIN_DEFAULT_PASSWORD=admin
